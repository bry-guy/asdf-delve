#!/usr/bin/env bash
set -eu
[ "${BASH_VERSINFO[0]}" -ge 3 ] && set -o pipefail

download() {
  local version=$1
  local download_path=$2

  git clone https://github.com/go-delve/delve "$download_path" --branch "v${version}"
}

download "$ASDF_INSTALL_VERSION" "$ASDF_DOWNLOAD_PATH"

